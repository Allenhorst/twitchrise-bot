version: "3.8"

services:
  twitchrise-bot:
    image: ghcr.io/driftywinds/twitchrise-bot:latest
    container_name: twitchrise-bot
    volumes:
      - /home/truenas_admin/files/watchlists.json:/app/watchlists.json
      - ./.env:/app/.env:ro
    restart: unless-stopped
    tty: true
